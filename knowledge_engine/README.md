# TLS_DSS/knowledge_engine

The knowledge engine of the TLS DSS is where the magic happens. The knowledge 
engine organizes the model output as well as uses expert rules (if/then) to inform
the content of the dialog submodule.
