# TLS_DSS/model_submodule

The model submodule of the TLS DSS is where the predictive neural network is 
stored. This NN is engaged by the knowledge engine, but is built on data in the
data submodule.
